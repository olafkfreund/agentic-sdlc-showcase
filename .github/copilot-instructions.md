Read `AGENTS.md` at the repository root and follow it. It is the source of truth for this
repository's context, commands, conventions and boundaries.

Also load the skills in `.agent/skills/` that match the task you were given.

This file is a pointer only. Do not add context here — add it to `AGENTS.md`, so it stays portable
across agent runtimes (playbook §4, principle 2).
