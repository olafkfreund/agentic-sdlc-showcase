# direnv: drop into the pinned toolchain on cd.
#   direnv allow
#
# Prefers devenv where it is installed, falls back to the flake. Both declare the
# same tools; flake.nix is canonical.
if has devenv; then
  use devenv
else
  use flake
fi
