"""FROZEN. Superseded by service/app/. Do not edit — the gate blocks it.

Kept so the frozen-path control has something real to protect, and so the
demo can show a gate refusing a change rather than describing one.
"""

LEGACY_ENDPOINTS = ["/v1/payment", "/v1/payment/status"]
